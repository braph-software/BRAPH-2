%% Pipeline Neural Networks Regression Cross Validation Structural Data
%
% This is the pipeline script to perform cross validation on regression by neural networks for one group of subjects with structural data.
% 1. It loads a brain atlas from an XLS file (e.g., desikan_atlas.xlsx).
% 2. It loads the data of two groups of subjects from one directory containing XLS file of each subject.
% 3. It constructs the dataset using structural data.
% 4. It performs the cross validation.

%% Brain Atlas
ba = ImporterBrainAtlasXLS('WAITBAR', true).get('BA'); % Load a Brain Atlas XLS % Brain Atlas

%% Groups
gr = ImporterGroupSubjectST_XLS('BA', ba, 'WAITBAR', true).get('GR'); % Load Group ST from XLS % Group ST

%% Dataset Construction
nnd = NNData_ST('GR', gr, 'TARGET_NAME', 'age'); % NN Dataset for Group % NN Group ST 

%% Cross Validation Setup
nnds = NNRegressorCrossValidation('GR', nnd.get('GR_NN')); % Set up the cross validation % Cross Validation