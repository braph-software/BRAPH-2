classdef Measurements
    properties (GetAccess = protected, SetAccess = protected)
    end
    methods (Access = protected)
        function m =  Measurements(atlas, group, varargin)
            % Should be an abstract class implemented by Measurements
            % subclasses
           
        end
    end 
end