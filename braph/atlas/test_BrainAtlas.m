% test BrainAtlas
br1 = BrainRegion('BR1', 'brain region 1', 1, 11, 111);
br2 = BrainRegion('BR2', 'brain region 2', 2, 22, 222);
br3 = BrainRegion('BR3', 'brain region 3', 3, 33, 333);
br4 = BrainRegion('BR4', 'brain region 4', 4, 44, 444);
br5 = BrainRegion('BR5', 'brain region 5', 5, 55, 555);
br6 = BrainRegion('BR6', 'brain region 6', 6, 66, 666);
br7 = BrainRegion('BR7', 'brain region 7', 7, 77, 777);
br8 = BrainRegion('BR8', 'brain region 8', 8, 88, 888);
br9 = BrainRegion('BR9', 'brain region 9', 9, 99, 999);

%% Test 1: Instantiation
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});

%% Test 2: Basic functionalities
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.brainregionnumber does not work')
assert(atlas.getBrainRegions().containsIndex(1), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.contains_brain_region does not work')
assert(~atlas.getBrainRegions().containsIndex(6), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.contains_brain_region does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(1).getLabel(), 'BR1'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.getBrainRegion does not work')

%% Test 3: Add
atlas = BrainAtlas('brain atlas', {br1, br2, br4, br5});

atlas.getBrainRegions().add(br3.getName(), br3, 3)

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.addBrainRegion does not work')
assert(isequal(atlas.getBrainRegions.getValueFromIndex(3).getLabel(), 'BR3'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.getBrainRegion does not work')

atlas.getBrainRegions().add(br6.getName(), br6);

assert(atlas.getBrainRegions().length()==6, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.addBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(6).getLabel(), 'BR6'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.addBrainRegion does not work')

%% Test 4: Remove
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
atlas.getBrainRegions().remove(3)

assert(atlas.getBrainRegions().length()==4, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(2).getLabel(), 'BR2'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(3).getLabel(), 'BR4'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegion does not work')

atlas.getBrainRegions().remove(1)

assert(atlas.getBrainRegions().length()==3, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(1).getLabel(), 'BR2'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegion does not work')

%% Test 5: Replace
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
atlas.getBrainRegions().replace(br9.getName(), br9, 3)

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.replaceBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(3).getLabel(), 'BR9'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.replaceBrainRegion does not work')

%% Test 6: Invert
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
atlas.getBrainRegions().invert(2, 4)

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.invertBrainRegions does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(2).getLabel(), 'BR4'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.invertBrainRegions does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(4).getLabel(), 'BR2'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.invertBrainRegions does not work')

%% Test 7: MoveTo
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
atlas.getBrainRegions().move_to(4, 2)

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.movetoBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(2).getLabel(), 'BR4'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.movetoBrainRegion does not work')

atlas.getBrainRegions().move_to(1, 5)

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.movetoBrainRegion does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(5).getLabel(), 'BR1'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.movetoBrainRegion does not work')

%% Test 8: Remove All
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
selected = atlas.getBrainRegions().remove_all([2, 4]);

assert(atlas.getBrainRegions().length()==3, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegions does not work')
assert(isequal(atlas.getBrainRegions().getValueFromIndex(3).getLabel(), 'BR5'), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegions does not work')
assert(isempty(selected), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.removeBrainRegions does not work')

%% Test 9: Move Up
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
selected = atlas.getBrainRegions().move_up([1 3 5]);

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.moveupBrainRegions does not work')
assert(isequal(selected, [1 2 4]), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.moveupBrainRegions does not work')

%% Test 10: Move Down
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
selected = atlas.getBrainRegions().move_down([1 3 5]);

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.moveupBrainRegions does not work')
assert(isequal(selected, [2 4 5]), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.moveupBrainRegions does not work')

%% Test 11: Move to Top
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
selected = atlas.getBrainRegions().move_to_top([1 3 5]);

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.move2topBrainRegions does not work')
assert(isequal(selected, [1 2 3]), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.move2topBrainRegions does not work')

%% Test 12: Move to Bottom
atlas = BrainAtlas('brain atlas', {br1, br2, br3, br4, br5});
selected = atlas.getBrainRegions().move_to_bottom([1 3 5]);

assert(atlas.getBrainRegions().length()==5, ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.move2bottomBrainRegions does not work')
assert(isequal(selected, [3 4 5]), ...
    'BRAPH:BrainAtlas:Bug', ...
    'BrainAtlas.move2bottomBrainRegions does not work')

%% Test 13: Save to XLS

br1 = BrainRegion('ISF', 'superiorfrontal', -12.6, 22.9, 42.4);
br2 = BrainRegion('lFP', 'frontalpole', -8.6,61.7,-8.7);
br3 = BrainRegion('lRMF', 'rostralmiddlefrontal', -31.3,41.2,16.5);
br4 = BrainRegion('lCMF', 'caudalmiddlefrontal', -34.6, 10.2, 42.8);
br5 = BrainRegion('lPOB', 'parsorbitalis', -41,38.8,-11.1);

atlas  = BrainAtlas('TestToSaveCoolName1', {br1, br2, br3, br4, br5});
BrainAtlas.save_to_xls(atlas);