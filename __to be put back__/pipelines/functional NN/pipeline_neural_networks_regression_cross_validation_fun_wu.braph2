%% Pipeline Neural Networks Regression Cross Validation FUN WU
%
% This is the pipeline script to perform cross validation on regression with age by neural networks for one group of subjects with functional data and weighted undirected graphs.
% 1. It loads a brain atlas from an XLS file (e.g., desikan_atlas.xlsx).
% 2. It loads the data of one group of subjects from directory containing XLS files.
% 3. It constructs the dataset using with functional matrices (FUN) based on graphs (WU).
% 4. It performs the cross validation.

%% Brain Atlas
ba = ImporterBrainAtlasXLS('WAITBAR', true).get('BA'); % Load a Brain Atlas XLS % Brain Atlas

%% Groups
gr = ImporterGroupSubjectCON_XLS('BA', ba, 'WAITBAR', true).get('GR'); % Load Group FUN from XLS % Group FUN

%% Dataset Construction
nnd = NNData_FUN_WU('GR', gr, 'TARGET_NAME', 'age'); % NN Dataset for Group FUN % NN Group FUN

%% Cross Validation Setup
nnds = NNRegressorCrossValidation('GR', nnd.get('GR_NN')); % Set up the cross validation % Cross Validation

