%TEST_IMPORTERBRAINATLASXLS

%% Test 1.1: Instantiation - empty

im = ImporterBrainAtlasXLS();

prop_number = ImporterBrainAtlasXLS.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	assert( ...
		isa(im.getr(prop), 'NoValue'), ...
		[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
		['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be inizialized to NoValue(). ' ...
		'Or there could be an error in ImporterBrainAtlasXLS.getr(' int2str(prop) ').'] ...
		)
	assert( ...
		isa(im.getr(TAG), 'NoValue'), ...
		[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
		['ImporterBrainAtlasXLS.getr(''' TAG ''') must be inizialized to NoValue(). ' ...
		'Or there could be an error in ImporterBrainAtlasXLS.getr(''' TAG ''').'] ...
		)
end

%% Test 1.2: Instantiation - defaults

warning('off', 'BRAPH2:ImporterBrainAtlasXLS')
im = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.ID), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.FILE), ...
	ImporterBrainAtlasXLS.BA, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.BA) ...
	);
warning('on', 'BRAPH2:ImporterBrainAtlasXLS')

for prop = 1:1:ImporterBrainAtlasXLS.getPropNumber()
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	switch ImporterBrainAtlasXLS.getPropCategory(prop)
		case {Category.METADATA, Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isequal(im.getr(prop), ImporterBrainAtlasXLS.getPropDefaultConditioned(prop)), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be inizialized to its default value ' ...
				'given by ImporterBrainAtlasXLS.getPropDefaultConditioned(' int2str(prop) '). ' ...
				'Or there could be an error in ImporterBrainAtlasXLS.getr(' int2str(prop) ').'] ...
				)
			assert( ...
				isequal(im.getr(TAG), ImporterBrainAtlasXLS.getPropDefaultConditioned(prop)), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(''' TAG ''') must be inizialized to its default value ' ...
				'given by ImporterBrainAtlasXLS.getPropDefaultConditioned(' int2str(prop) '). ' ...
				'Or there could be an error in ImporterBrainAtlasXLS.getr(''' TAG ''').'] ...
				)
		case Category.RESULT
			assert( ...
				isa(im.getr(prop), 'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['Being a result, ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be inizialized to NoValue(). ' ...
				'Or there could be an error in ImporterBrainAtlasXLS.getr(' int2str(prop) ').'] ...
				)
			assert( ...
				isa(im.getr(TAG), 'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['Being a result, ImporterBrainAtlasXLS.getr(''' TAG ''') must be inizialized to NoValue(). ' ...
				'Or there could be an error in ImporterBrainAtlasXLS.getr(''' TAG ''').'] ...
				)
	end
end

%% Test 2: Callbacks

warning('off', 'BRAPH2:ImporterBrainAtlasXLS')
im_0 = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.ID), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.FILE), ...
	ImporterBrainAtlasXLS.BA, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.BA) ...
	);
warning('on', 'BRAPH2:ImporterBrainAtlasXLS')

im_1 = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, Callback('EL', im_0, 'PROP', 1), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, Callback('EL', im_0, 'PROP', 5), ...
	ImporterBrainAtlasXLS.BA, NoValue() ...
	);
	
prop_number = ImporterBrainAtlasXLS.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	switch ImporterBrainAtlasXLS.getPropCategory(prop)
		case {Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isa(im_1.getr(prop),  'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be a callback.'] ...
				)
			assert( ...
				isequal(im_1.get(prop), im_0.get(prop)) && ~isa(im_1.get(prop), 'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.get(' int2str(prop) ') must be the actual property and NOT a callback.'] ...
				)
		case {Category.METADATA, Category.RESULT}
			assert( ...
				~isa(im_1.getr(prop), 'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must NOT be a callback.'] ...
				)
	end
end
im_2 = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, Callback('EL', im_1, 'PROP', 1), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, Callback('EL', im_1, 'PROP', 5), ...
	ImporterBrainAtlasXLS.BA, NoValue() ...
	);
	
prop_number = ImporterBrainAtlasXLS.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	switch ImporterBrainAtlasXLS.getPropCategory(prop)
		case {Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isa(im_2.getr(prop),  'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be a callback.'] ...
				)
			assert( ...
				isequal(im_2.get(prop), im_0.get(prop)) && ~isa(im_1.get(prop), 'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.get(' int2str(prop) ') must be the actual property and NOT a callback.'] ...
				)
		case {Category.METADATA, Category.RESULT}
			assert( ...
				~isa(im_2.getr(prop), 'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must NOT be a callback.'] ...
				)
	end
end
im_3 = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, Callback('EL', im_2, 'PROP', 1), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, Callback('EL', im_2, 'PROP', 5), ...
	ImporterBrainAtlasXLS.BA, NoValue() ...
	);
	
prop_number = ImporterBrainAtlasXLS.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	switch ImporterBrainAtlasXLS.getPropCategory(prop)
		case {Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isa(im_3.getr(prop),  'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be a callback.'] ...
				)
			assert( ...
				isequal(im_3.get(prop), im_0.get(prop)) && ~isa(im_1.get(prop), 'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.get(' int2str(prop) ') must be the actual property and NOT a callback.'] ...
				)
		case {Category.METADATA, Category.RESULT}
			assert( ...
				~isa(im_3.getr(prop), 'Callback'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must NOT be a callback.'] ...
				)
	end
end

%% Test 3: Result

warning('off', 'BRAPH2:ImporterBrainAtlasXLS')
im = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.ID), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.FILE), ...
	ImporterBrainAtlasXLS.BA, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.BA) ...
	);
warning('on', 'BRAPH2:ImporterBrainAtlasXLS')

prop_number = ImporterBrainAtlasXLS.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	switch ImporterBrainAtlasXLS.getPropCategory(prop)
		case {Category.METADATA, Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
		case Category.RESULT
			assert( ...
				~isa(im.get(prop),  'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.get(' int2str(prop) ') must NOT be a NoValue, because it should have been calculated.'] ...
				)
			assert( ...
				isa(im.getr(prop),  'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be a NoValue, because it has not been memorized.'] ...
				)
			if isempty(im.getPropSettings(prop))
				assert( ...
					im.checkFormat(im.getPropFormat(prop), im.get(prop)), ...
					[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
					['ImporterBrainAtlasXLS.get(' int2str(prop) ') returns a value with the wrong format.'] ...
					)
			else
				assert( ...
					im.checkFormat(im.getPropFormat(prop), im.get(prop), im.getPropSettings(prop)), ...
					[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
					['ImporterBrainAtlasXLS.get(' int2str(prop) ') returns a value with the wrong format.'] ...
					)
			end
	end
end

%% Test 4: Memorize

warning('off', 'BRAPH2:ImporterBrainAtlasXLS')
im = ImporterBrainAtlasXLS( ...
	ImporterBrainAtlasXLS.ID, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.ID), ...
	ImporterBrainAtlasXLS.LABEL, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.LABEL), ...
	ImporterBrainAtlasXLS.NOTES, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.NOTES), ...
	ImporterBrainAtlasXLS.WAITBAR, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.WAITBAR), ...
	ImporterBrainAtlasXLS.FILE, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.FILE), ...
	ImporterBrainAtlasXLS.BA, ImporterBrainAtlasXLS.getPropDefault(ImporterBrainAtlasXLS.BA) ...
	);
warning('on', 'BRAPH2:ImporterBrainAtlasXLS')

for prop = 1:1:ImporterBrainAtlasXLS.getPropNumber()
	TAG = upper(ImporterBrainAtlasXLS.getPropTag(prop));
	switch ImporterBrainAtlasXLS.getPropCategory(prop)
		case {Category.METADATA, Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
		case Category.RESULT
			assert( ...
				~isa(im.get(prop),  'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.get(' int2str(prop) ') must NOT be a NoValue, because it should have been calculated.'] ...
				)
			assert( ...
				isa(im.getr(prop),  'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must be a NoValue, because it should NOT have been memorized.'] ...
				)
			assert( ...
				~isa(im.memorize(prop),  'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.memorize(' int2str(prop) ') must NOT be a NoValue, because it should have been calculated.'] ...
				)
			assert( ...
				~isa(im.getr(prop),  'NoValue'), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') must NOT be a NoValue, because it should have been memorized.'] ...
				)
			assert( ...
				im.checkFormat(im.getPropFormat(prop), im.getr(prop),im.getPropSettings(prop)), ...
				[BRAPH2.STR ':ImporterBrainAtlasXLS:' BRAPH2.BUG_FUNC], ...
				['ImporterBrainAtlasXLS.getr(' int2str(prop) ') returns a value with the wrong format.'] ...
				)
	end
end

