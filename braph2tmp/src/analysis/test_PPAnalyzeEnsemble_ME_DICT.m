%TEST_PPANALYZEENSEMBLE_ME_DICT

%% Test 1.1: Instantiation - empty

pr = PPAnalyzeEnsemble_ME_DICT();

prop_number = PPAnalyzeEnsemble_ME_DICT.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	assert( ...
		isa(pr.getr(prop), 'NoValue'), ...
		[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
		['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be inizialized to NoValue(). ' ...
		'Or there could be an error in PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ').'] ...
		)
	assert( ...
		isa(pr.getr(TAG), 'NoValue'), ...
		[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
		['PPAnalyzeEnsemble_ME_DICT.getr(''' TAG ''') must be inizialized to NoValue(). ' ...
		'Or there could be an error in PPAnalyzeEnsemble_ME_DICT.getr(''' TAG ''').'] ...
		)
end

%% Test 1.2: Instantiation - defaults

warning('off', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')
pr = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ID), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.PROP), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
warning('on', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')

for prop = 1:1:PPAnalyzeEnsemble_ME_DICT.getPropNumber()
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	switch PPAnalyzeEnsemble_ME_DICT.getPropCategory(prop)
		case {Category.METADATA, Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isequal(pr.getr(prop), PPAnalyzeEnsemble_ME_DICT.getPropDefaultConditioned(prop)), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be inizialized to its default value ' ...
				'given by PPAnalyzeEnsemble_ME_DICT.getPropDefaultConditioned(' int2str(prop) '). ' ...
				'Or there could be an error in PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ').'] ...
				)
			assert( ...
				isequal(pr.getr(TAG), PPAnalyzeEnsemble_ME_DICT.getPropDefaultConditioned(prop)), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(''' TAG ''') must be inizialized to its default value ' ...
				'given by PPAnalyzeEnsemble_ME_DICT.getPropDefaultConditioned(' int2str(prop) '). ' ...
				'Or there could be an error in PPAnalyzeEnsemble_ME_DICT.getr(''' TAG ''').'] ...
				)
		case Category.RESULT
			assert( ...
				isa(pr.getr(prop), 'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['Being a result, PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be inizialized to NoValue(). ' ...
				'Or there could be an error in PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ').'] ...
				)
			assert( ...
				isa(pr.getr(TAG), 'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['Being a result, PPAnalyzeEnsemble_ME_DICT.getr(''' TAG ''') must be inizialized to NoValue(). ' ...
				'Or there could be an error in PPAnalyzeEnsemble_ME_DICT.getr(''' TAG ''').'] ...
				)
	end
end

%% Test 2: Callbacks

warning('off', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')
pr_0 = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ID), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.PROP), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
warning('on', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')

pr_1 = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, Callback('EL', pr_0, 'PROP', 1), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, Callback('EL', pr_0, 'PROP', 4), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
	
prop_number = PPAnalyzeEnsemble_ME_DICT.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	switch PPAnalyzeEnsemble_ME_DICT.getPropCategory(prop)
		case {Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isa(pr_1.getr(prop),  'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be a callback.'] ...
				)
			assert( ...
				isequal(pr_1.get(prop), pr_0.get(prop)) && ~isa(pr_1.get(prop), 'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') must be the actual property and NOT a callback.'] ...
				)
		case {Category.METADATA, Category.RESULT}
			assert( ...
				~isa(pr_1.getr(prop), 'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must NOT be a callback.'] ...
				)
	end
end
pr_2 = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, Callback('EL', pr_1, 'PROP', 1), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, Callback('EL', pr_1, 'PROP', 4), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
	
prop_number = PPAnalyzeEnsemble_ME_DICT.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	switch PPAnalyzeEnsemble_ME_DICT.getPropCategory(prop)
		case {Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isa(pr_2.getr(prop),  'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be a callback.'] ...
				)
			assert( ...
				isequal(pr_2.get(prop), pr_0.get(prop)) && ~isa(pr_1.get(prop), 'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') must be the actual property and NOT a callback.'] ...
				)
		case {Category.METADATA, Category.RESULT}
			assert( ...
				~isa(pr_2.getr(prop), 'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must NOT be a callback.'] ...
				)
	end
end
pr_3 = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, Callback('EL', pr_2, 'PROP', 1), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, Callback('EL', pr_2, 'PROP', 4), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
	
prop_number = PPAnalyzeEnsemble_ME_DICT.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	switch PPAnalyzeEnsemble_ME_DICT.getPropCategory(prop)
		case {Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
			assert( ...
				isa(pr_3.getr(prop),  'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be a callback.'] ...
				)
			assert( ...
				isequal(pr_3.get(prop), pr_0.get(prop)) && ~isa(pr_1.get(prop), 'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') must be the actual property and NOT a callback.'] ...
				)
		case {Category.METADATA, Category.RESULT}
			assert( ...
				~isa(pr_3.getr(prop), 'Callback'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must NOT be a callback.'] ...
				)
	end
end

%% Test 3: Result

warning('off', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')
pr = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ID), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.PROP), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
warning('on', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')

prop_number = PPAnalyzeEnsemble_ME_DICT.getPropNumber();
for prop = 1:1:prop_number
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	switch PPAnalyzeEnsemble_ME_DICT.getPropCategory(prop)
		case {Category.METADATA, Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
		case Category.RESULT
			assert( ...
				~isa(pr.get(prop),  'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') must NOT be a NoValue, because it should have been calculated.'] ...
				)
			assert( ...
				isa(pr.getr(prop),  'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be a NoValue, because it has not been memorized.'] ...
				)
			if isempty(pr.getPropSettings(prop))
				assert( ...
					pr.checkFormat(pr.getPropFormat(prop), pr.get(prop)), ...
					[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
					['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') returns a value with the wrong format.'] ...
					)
			else
				assert( ...
					pr.checkFormat(pr.getPropFormat(prop), pr.get(prop), pr.getPropSettings(prop)), ...
					[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
					['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') returns a value with the wrong format.'] ...
					)
			end
	end
end

%% Test 4: Memorize

warning('off', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')
pr = PPAnalyzeEnsemble_ME_DICT( ...
	PPAnalyzeEnsemble_ME_DICT.ID, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ID), ...
	PPAnalyzeEnsemble_ME_DICT.BKGCOLOR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.BKGCOLOR), ...
	PPAnalyzeEnsemble_ME_DICT.EL, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.EL), ...
	PPAnalyzeEnsemble_ME_DICT.PROP, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.PROP), ...
	PPAnalyzeEnsemble_ME_DICT.TITLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.TITLE), ...
	PPAnalyzeEnsemble_ME_DICT.ENABLE, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.ENABLE), ...
	PPAnalyzeEnsemble_ME_DICT.WAITBAR, PPAnalyzeEnsemble_ME_DICT.getPropDefault(PPAnalyzeEnsemble_ME_DICT.WAITBAR) ...
	);
warning('on', 'BRAPH2:PPAnalyzeEnsemble_ME_DICT')

for prop = 1:1:PPAnalyzeEnsemble_ME_DICT.getPropNumber()
	TAG = upper(PPAnalyzeEnsemble_ME_DICT.getPropTag(prop));
	switch PPAnalyzeEnsemble_ME_DICT.getPropCategory(prop)
		case {Category.METADATA, Category.PARAMETER, Category.DATA, Category.FIGURE, Category.GUI}
		case Category.RESULT
			assert( ...
				~isa(pr.get(prop),  'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.get(' int2str(prop) ') must NOT be a NoValue, because it should have been calculated.'] ...
				)
			assert( ...
				isa(pr.getr(prop),  'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must be a NoValue, because it should NOT have been memorized.'] ...
				)
			assert( ...
				~isa(pr.memorize(prop),  'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.memorize(' int2str(prop) ') must NOT be a NoValue, because it should have been calculated.'] ...
				)
			assert( ...
				~isa(pr.getr(prop),  'NoValue'), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') must NOT be a NoValue, because it should have been memorized.'] ...
				)
			assert( ...
				pr.checkFormat(pr.getPropFormat(prop), pr.getr(prop),pr.getPropSettings(prop)), ...
				[BRAPH2.STR ':PPAnalyzeEnsemble_ME_DICT:' BRAPH2.BUG_FUNC], ...
				['PPAnalyzeEnsemble_ME_DICT.getr(' int2str(prop) ') returns a value with the wrong format.'] ...
				)
	end
end

