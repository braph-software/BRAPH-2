%% Pipeline Neural Networks Classification Cross Validation Functional WU
%
% This is the pipeline script to perform cross validation on classification by neural networks for two groups of subjects with functional data and weighted undirected graphs.
% 1. It loads a brain atlas from an XLS file (e.g., aal90_atlas.xlsx).
% 2. It loads the data of two groups of subjects from two directories (e.g., GroupName1 and GroupName2) containing XLS files of each subject.
% 3. It constructs the dataset using connectivity matrices (FUN) based on graphs (WU).
% 4. It performs the cross validation.

%% Brain Atlas
ba = ImporterBrainAtlasXLS('WAITBAR', true).get('BA'); % Load a Brain Atlas XLS % Brain Atlas

%% Groups
gr1 = ImporterGroupSubjectFUN_XLS('BA', ba, 'WAITBAR', true).get('GR'); % Load Group FUN 1 from XLS % Group FUN 1
gr2 = ImporterGroupSubjectFUN_XLS('BA', ba, 'WAITBAR', true).get('GR'); % Load Group FUN 2 from XLS % Group FUN 2

%% Dataset Construction
nnd1 = NNData_FUN_WU('GR', gr1, 'TARGET_NAME', gr1.get('ID')); % NN Dataset for Group FUN 1 % NN Group FUN 1
nnd2 = NNData_FUN_WU('GR', gr2, 'TARGET_NAME', gr2.get('ID'), 'TEMPLATE', nnd1); % NN Dataset for Group FUN 2 % NN Group FUN 2

%% Cross Validation Setup
nnds = NNClassifierCrossValidation('GR1', nnd1.get('GR_NN'), 'GR2', nnd2.get('GR_NN')); % Set up the cross validation % Cross Validation

