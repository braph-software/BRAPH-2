%% Pipeline Neural Networks Classification Cross Validation Multiple Structural Data
%
% This is the pipeline script to perform cross validation on classification by neural networks for two groups of subjects with multiple structural data.
% 1. It loads a brain atlas from an XLS file (e.g., desikan_atlas.xlsx).
% 2. It loads the data of two groups of subjects from two directories (e.g., GroupName1 and GroupName2) containing XLS files of each subject.
% 3. It constructs the dataset using structural data.
% 4. It performs the cross validation.

%% Brain Atlas
ba = ImporterBrainAtlasXLS('WAITBAR', true).get('BA'); % Load a Brain Atlas XLS % Brain Atlas

%% Groups
gr1 = ImporterGroupSubjectST_MP_XLS('BA', ba, 'WAITBAR', true).get('GR'); % Load Group ST MP 1 from XLS % Group ST 1
gr2 = ImporterGroupSubjectST_MP_XLS('BA', ba, 'WAITBAR', true).get('GR'); % Load Group ST MP 2 from XLS % Group ST 2

%% Dataset Construction
nnd1 = NNData_ST_MM('GR', gr1, 'TARGET_NAME', gr1.get('ID')); % NN Dataset for Group 1 % NN Group ST 1
nnd2 = NNData_ST_MM('GR', gr2, 'TARGET_NAME', gr2.get('ID'), 'TEMPLATE', nnd1); % NN Dataset for Group 2 % NN Group ST 2

%% Cross Validation Setup
nnds = NNClassifierCrossValidation('GR1', nnd1.get('GR_NN'), 'GR2', nnd2.get('GR_NN')); % Set up the cross validation % Cross Validation